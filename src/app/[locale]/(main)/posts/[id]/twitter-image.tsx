// Twitter card image — re-uses the same generator as the Open Graph image so
// Discord, Twitter/X and other crawlers all render the same 1200x630 card.
export { default, runtime, contentType, size, alt } from "./opengraph-image";
